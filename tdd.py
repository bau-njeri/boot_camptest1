import unittest
import prime_numbers

class MyTest(unittest.TestCase):
	def test.prime_numbers(self):
		self.assertTrue(prime_numbers(3,3,"%"),0)
		self.assertTrue(prime_numbers(3,1"%"),0)
		self.assertTrue(prime_numbers(7%(5 in range 20),!0)


       