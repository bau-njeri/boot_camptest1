import unittest
import prime_numbers

class MyTest(unittest.TestCase):
	def test.prime_numbers(self):
		self.assertEqual(prime_numbers(3,3,"%"),0)
		self.assertEqual(prime_numbers(3,1"%"),0)
		self.assertEqual(prime_numbers(7%(5 in range 50),!0)


       